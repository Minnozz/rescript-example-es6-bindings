{
  "scripts": {
    "prepare": "rescript && esbuild ./lib/es6/src/demo.js --bundle --outfile=bundle.js"
  },
  "dependencies": {
    "date-fns": "^2.22.1",
    "esbuild": "^0.12.5",
    "rescript": "^9.1.2"
  }
}
